<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Devnics</title>
</head>
<body>
    <% for (product of products) { %>
        <a href="/store/<%= product.id %>">
            <div>
                <img src="LogoTransparent.png" alt="Purchase Product" width="50px" height="50px">
                <h3><%= product.name %></h2>
                <p>$<%= product.price %></p>
                <a href="/store/<%= product.id %>/purchase">
                    <button>Buy</button>
                </a>
            </div>
        </a>
    <% } %>
</body>
</html>